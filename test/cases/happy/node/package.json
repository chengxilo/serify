{
  "name": "happy-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm install && npx tsc -p node_modules/@chengxilo/serify && npx tsc"
  },
  "dependencies": {
    "@chengxilo/serify": "file:../../../../lib/node"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
